source "https://rubygems.org"

# Mirrors the gem set GitHub Pages uses to build *.github.io sites
# (Jekyll 3.9.x, jekyll-theme-cayman, supported plugins).
gem "github-pages", group: :jekyll_plugins

# Ruby 3.x no longer ships webrick in stdlib; Jekyll's local server needs it.
gem "webrick"
